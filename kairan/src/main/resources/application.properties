spring.application.name=kairan_db


# MySQLの設定
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/kairan_db
spring.datasource.username=root
spring.datasource.password=9eRxj!*$~X7-
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.encoding=utf-8
spring.sql.init.mode=always

# Hibernateの設定（スキーマ自動作成）
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# Thymeleafの設定（キャッシュ無効化）
spring.thymeleaf.cache=false

# ログレベルの設定（デバッグモード）
# Spring のログレベル
logging.level.org.springframework=INFO
# Hibernate の SQL ログを詳細に表示
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


# Spring Security のデフォルトログイン設定
spring.security.user.name=admin
spring.security.user.password=admin123

# Mailtrap SMTP 設定
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=06c74591135f8d
spring.mail.password=9aec905f069d9e


# ---【Stripe APIキー設定】--- 
stripe.api.key=${STRIPE_API_KEY} 

# その他の設定
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# 保存ファイルのサイズ上限を制限
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB



